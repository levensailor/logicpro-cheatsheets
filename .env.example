# Vercel AI Gateway key for Logic Pro Guru chat (`/api/chat`).
# Create a key in the Vercel dashboard (AI Gateway) and paste it here for local dev.
# Docs: https://vercel.com/docs/ai-gateway
AI_GATEWAY_API_KEY=your-ai-gateway-api-key-here

# Optional: direct OpenAI is no longer used by `/api/chat` once Gateway is configured.
